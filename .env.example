# Authentication (choose one)
 #
 # Option A (recommended): Vertex AI (uses ADC)
 # - For local dev: run `gcloud auth application-default login`
 # - Then set:
 GOOGLE_CLOUD_PROJECT=your_gcp_project_id
 GOOGLE_CLOUD_LOCATION=us-central1
 #
 # Option B: Google AI Studio (API key)
 # - Get your key from: https://aistudio.google.com/app/apikey
 # - Then set:
 # GOOGLE_API_KEY=your_google_api_key_here

# Backend selection is automatic:
# - If GOOGLE_API_KEY is set -> AI Studio (Gemini API)
# - Else if GOOGLE_CLOUD_PROJECT + GOOGLE_CLOUD_LOCATION are set -> Vertex AI

# Optional model configuration
# Default primary: gemini-2.5-flash
# Default fallback (used on transient 503 overload errors and for quota fallback): gemini-2.5-flash-lite
#
# GEMINI_MODEL=gemini-2.5-flash
# GEMINI_FALLBACK_MODEL=gemini-2.5-flash-lite
